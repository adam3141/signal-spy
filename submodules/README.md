# Submodule usage
This directory contains the submodules for the project. It is recommended to use CMake's FetchContent module to download and build the external dependencies rather than using git submodules.

using `git submodule` can be used in circumstances where you might need to make changes to the code.
